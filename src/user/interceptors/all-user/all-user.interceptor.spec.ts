import { UserDataInterceptor } from './all-user.interceptor';

describe('UserDataInterceptor', () => {
  it('should be defined', () => {
    expect(new UserDataInterceptor()).toBeDefined();
  });
});
